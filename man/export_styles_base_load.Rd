% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/export_docx_styles_load.R
\name{export_styles_base_load}
\alias{export_styles_base_load}
\title{Load base styles matrix for .docx transcripts}
\usage{
export_styles_base_load(path = NULL, path_docx = NA)
}
\arguments{
\item{path}{Character string; path to the styles file (.xlsx or .csv).
If \code{NULL}, the default styles file shipped with act is used.}

\item{path_docx}{Character string; Path to a .docx template file. If given,
it will be checked if all styles defined in the matrix are present in the
.docx file. If \code{NA} nothing will be checked.}
}
\value{
Data.frame with columns \code{act.style.name} and \code{docx.template.name}.
}
\description{
Loads the base styles matrix from an XLSX file (sheet \code{base}) or
a CSV file (semicolon-delimited). The matrix maps act-internal style names
to DOCX template paragraph style names.
}
\examples{
library(act)

# An example replacement matrix comes with the package.
path <- system.file("extdata", "normalization", "normalization_matrix.csv", package="act")

# Load the matrix
mymatrix <- act::matrix_load(path)

# Have a look at the matrix
colnames(mymatrix)
mymatrix

#the original path of the matrix is stored in the attributes
attr(mymatrix, 'path') 
}
\seealso{
\code{vignette("export_docx_styles", package = "act")}
}
