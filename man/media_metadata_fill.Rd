% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/media_helpers.R
\name{media_metadata_fill}
\alias{media_metadata_fill}
\title{Fill the read-derived columns of a media table from the files}
\usage{
media_metadata_fill(media, force = FALSE)
}
\arguments{
\item{media}{Data.frame; a media table as built by \code{media_build}.}

\item{force}{Logical; if \code{TRUE} re-reads every row.}
}
\value{
Data.frame; the media table with read columns filled.
}
\description{
For each row whose metadata has not been read yet (\code{length.sec} is
\code{NA}, unless \code{force = TRUE}), reads the file via
\code{media_metadata_read} and fills \code{length.sec}, \code{source.path},
\code{clip.id} and the \code{video.*} columns. \code{startsec}/
\code{startsec.source} are only overwritten when the file actually carries an
offset (tag or timecode), so the \code{0} of full recordings is preserved.
}
\seealso{
\link{media_build}, \link{media_metadata_read}, \link{media_assign}
}
