% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/metadata_helpers.R
\name{helper_metadata_exif_write}
\alias{helper_metadata_exif_write}
\title{Write act.* tags into the EXIF UserComment of a JPG file}
\usage{
helper_metadata_exif_write(file, sourcePath, startsec, clipID)
}
\arguments{
\item{file}{Character string. Path to the JPG file to tag.}

\item{sourcePath}{Character string. Full path to the source media file
the still was extracted from.}

\item{startsec}{Numeric. Time in original source media, in seconds,
where the still frame was captured.}

\item{clipID}{Character string. Cut identifier.}
}
\value{
Invisibly \code{NULL}.
}
\description{
Uses the bundled exiftool from the \code{exifr} package (which itself
requires Perl on the system). When \code{exifr} is not installed or \code{perl}
is not on the system path, the function silently returns without
modifying the file. The single setup hint is emitted by
\code{iclo::config()} (not by this helper, to keep it usable as a quiet
library primitive).
}
\details{
Uses \code{-overwrite_original} to avoid \code{.jpg_original} backup files.
}
\seealso{
\code{\link[=media_metadata_read]{media_metadata_read()}}, \code{\link[=helper_metadata_ffmpeg_args]{helper_metadata_ffmpeg_args()}}
}
