% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/export_docx_styles_load.R
\name{export_styles_user_load}
\alias{export_styles_user_load}
\title{Load user formatting rules matrix for .docx transcripts}
\usage{
export_styles_user_load(path = NULL)
}
\arguments{
\item{path}{Character string; path to the styles file (.xlsx or .csv).
If \code{NULL}, the default styles file shipped with act is used.}
}
\value{
Data.frame with tier formatting rules.
}
\description{
Loads the user formatting rules matrix from an XLSX file (sheet \code{user}) or
a CSV file (semicolon-delimited). The matrix defines per-tier rendering
rules for the DOCX export: visibility, line numbers, speaker acronyms,
paragraph style, indentation, and spacing.
}
\details{
Each row in the matrix defines formatting rules for tiers matching a regex
pattern or for a named layer column. The first matching row wins.

\strong{Columns:}

\emph{Identification:}
\itemize{
\item \code{name} -- Character. Human-readable label for the rule (informational only).
\item \code{match.regex} -- Character. Regular expression matched against tier names
or internal style identifiers. First matching rule wins. Each row must
have a \code{match.regex} value.

Tier names: matched against \code{tierName} in annotations (e.g. \verb{#mm-body},
\verb{^cut}). Internal identifiers: \verb{^space$} (spacer rows between blocks).
}

\emph{Visibility:}
\itemize{
\item \code{show} -- Logical. \code{TRUE} to render the tier/layer, \code{FALSE} to hide it.
Default: \code{TRUE}.
}

\emph{Paragraph style:}
\itemize{
\item \code{docx.template.name} -- Character. Name of a paragraph style defined in
the .docx template file. If \code{NA}, uses the default transcript style.
}

\emph{Line numbers:}
\itemize{
\item \code{line.nr.show} -- Logical. \code{TRUE} to show line numbers, \code{FALSE} to
replace them with spaces (preserving alignment). Default: \code{TRUE}.
}

\emph{Speaker acronym:}
\itemize{
\item \code{acronym.show} -- Logical. Show (\code{TRUE}) or hide (\code{FALSE}) the speaker
acronym. Default: \code{TRUE}.
\item \code{acronym.case} -- Character. Case transformation for the acronym.
Allowed: \code{"toupper"}, \code{"tolower"}, \code{"capitalize"}. Default: no change.
\item \code{acronym.search} -- Character. Regex search pattern applied to the
acronym text.
\item \code{acronym.replace} -- Character. Replacement string for
\code{acronym.search} matches.
\item \code{acronym.width} -- Numeric. Fixed character width for the acronym
(padded/truncated). \code{0} = use global layout default. Default: \code{0}.
\item \code{acronym.ending} -- Character. String appended after the acronym
(e.g., \code{":"} or \code{" "}). If \code{NA}, uses the layout default.
}

\emph{Indentation (for layers):}
\itemize{
\item \code{content.indent} -- Character. Controls indentation of layer lines.
Allowed values:
\itemize{
\item \code{"none"} -- No indent, content starts at the left margin.
\item \code{"content"} -- Align under the content column (after prefix).
\item \code{"text"} -- Align under the first text character of the main
annotation. Use \code{content.indent.text.skip} to skip leading symbols.
\item \code{"align"} -- Align symbols in the layer under corresponding symbols
in the main annotation using \code{content.indent.align.char}.
Default: \code{"none"}.
}
\item \code{content.indent.text.skip} -- Character. Regex pattern for characters
at the beginning of the main annotation to skip when calculating
indent position for \code{content.indent = "text"}. For example,
\code{=}, \code{[}, breath symbols, or pauses that should not affect
the alignment of layer text.
\item \code{content.indent.align.char} -- Character. One or more characters used
as alignment targets for \code{content.indent = "align"}. Each character
in the string is treated as a separate symbol to align.
\item \code{content.indent.align.filler.inside} -- Character. Single character
used as filler inside brackets when adjusting bracket lengths during
alignment. Default: space \code{" "}. Must be exactly one character.
\item \code{content.indent.align.mode} -- Character. Alignment mode for
\code{content.indent = "align"}. Allowed values:
\itemize{
\item \code{"bracket"} -- Pairwise symbols (e.g. \verb{|...|}, \verb{+...+}).
Filler inside brackets uses \code{content.indent.align.filler.inside}.
\item \code{"point"} -- Single symbol marking a point in time (e.g. \verb{#01}).
Only the symbol position is aligned, no bracket filling.
}
}

\emph{Wrapping:}
\itemize{
\item \code{content.wrap} -- Logical. \code{TRUE} to wrap text at the transcript width,
\code{FALSE} to allow text to extend beyond the column width without
line breaks. Default: \code{TRUE}. Ignored for \verb{^space$} rows.
}

\emph{Spacing:}
\itemize{
\item \code{space.after} -- Character. Controls spacing after this tier's annotations.
Allowed values:
\itemize{
\item \code{"block"} -- Insert an empty paragraph after the end of the block
(after the last visible line that belongs to the same main annotation).
This is the typical setting for transcript tiers.
\item \code{"always"} -- Insert an empty paragraph after every line of this tier,
even if more lines in the same block follow.
\item \code{"no"} -- No spacing after this tier.
Default: \code{NA} (no spacing).
}
}

\emph{Other:}
\itemize{
\item \code{comment} -- Character. Free-text comment (ignored by the code).
}
}
\seealso{
\code{vignette("export_docx_styles", package = "act")}
}
