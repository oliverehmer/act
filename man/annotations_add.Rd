% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/annotations_add.R
\name{annotations_add}
\alias{annotations_add}
\title{Add annotation to transcript with overlap handling}
\usage{
annotations_add(
  x,
  tierName,
  startsec,
  endsec,
  content = "",
  content.norm = "",
  overwrite = FALSE
)
}
\arguments{
\item{x}{Transcript object.}

\item{tierName}{Character string; name of the tier (exact name, not regex).}

\item{startsec}{Numeric; start time in seconds.}

\item{endsec}{Numeric; end time in seconds.}

\item{content}{Character string; content of the annotation. Default \code{""}.}

\item{content.norm}{Character string; normalized content. Default \code{""}.}

\item{overwrite}{Logical; if \code{FALSE}, throws error when overlap exists.
If \code{TRUE}, trims/splits existing overlapping annotations. Default \code{FALSE}.}
}
\value{
Transcript object with added annotation.
}
\description{
Adds a single annotation (interval or point) to a transcript object.
Automatically checks for overlapping annotations and handles them based on
the overwrite parameter. Always ensures no invalid overlaps exist.
}
\details{
\subsection{Overlap Handling}{

The function always checks for overlapping annotations in the specified tier:

\strong{When \code{overwrite = FALSE} (default):}
\itemize{
\item If overlap exists → Error with \code{stop()}
\item If no overlap → Insert new annotation
}

\strong{When \code{overwrite = TRUE}:}
\itemize{
\item Existing overlapping annotations are trimmed/split:
\itemize{
\item Left remainder: \code{[old_start, new_start]} (if \code{old_start < new_start})
\item Right remainder: \code{[new_end, old_end]} (if \code{new_end < old_end})
\item Middle part is replaced by new annotation
}
\item New annotation is inserted
}
}

\subsection{Automatic Operations}{
\itemize{
\item Assigns unique annotation ID
\item Sets all char.* fields to NA (filled by fulltext operations)
\item Maintains proper data.frame structure
\item Updates modification time and history
}
}
}
\examples{
\dontrun{
library(act)

# Add annotation (error if overlap exists)
t <- annotations_add(
  x = examplecorpus@transcripts[[1]],
  tierName = "status",
  startsec = 10.5,
  endsec = 15.2,
  content = "o0.s0"
)

# Add annotation and overwrite overlaps
t <- annotations_add(
  x = examplecorpus@transcripts[[1]],
  tierName = "status",
  startsec = 10.5,
  endsec = 15.2,
  content = "new content",
  overwrite = TRUE
)
}
}
