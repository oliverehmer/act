% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/metadata_helpers.R
\name{media_metadata_read}
\alias{media_metadata_read}
\title{Read act.* metadata and container properties from media files}
\usage{
media_metadata_read(file, tolerance_sec = 0.05)
}
\arguments{
\item{file}{Character. Single file path or a vector of paths.}

\item{tolerance_sec}{Numeric. Absolute tolerance in seconds for the
consistency check between \code{act.startsec} tag and timecode track.
Default is \code{0.05}.}
}
\value{
A data.frame with one row per input file.
}
\description{
Universal reader for media files. Supported formats: mp4, mov, wav, mp3
(via ffprobe) and jpg, jpeg (via exifr/exiftool). Returns a data.frame
with one row per file. All columns are always present; columns that do
not apply to the file format are \code{NA}.
}
\details{
Column groups:
\itemize{
\item act tag columns: \code{source}, \code{source.exists}, \code{startsec},
\code{startsec.source}, \code{clip.id}, \code{timecode}, \code{comment}
\item container columns: \code{container.format}, \code{length.sec}, \code{file.size},
\code{mtime}
\item video columns: \code{video.width}, \code{video.height}, \code{video.fps},
\code{video.codec}, \code{video.bitrate}
\item audio columns: \code{audio.sample.rate}, \code{audio.channels}, \code{audio.codec},
\code{audio.bitrate}
\item image columns: \code{image.width}, \code{image.height}, \code{bit.depth}
}

The \code{comment} column carries consistency notes joined by \verb{; }. Examples:
\code{"ok"}, \code{"no tags, using timecode"}, \code{"no tags, no timecode"},
\code{"source missing on disk"}, \code{"INCONSISTENT: tag=X.XXX timecode=Y.YYY"},
\code{"INCONSISTENT: clipID tag=X file=Y"}, \code{"clipID derived from filename"},
\code{"unsupported format"}.

Priority for \code{startsec}: \code{act.startsec} tag wins. If absent, the
timecode track value is used (MP4 tmcd stream). If neither, \code{NA}.
}
\seealso{
\code{\link[=helper_metadata_ffmpeg_args]{helper_metadata_ffmpeg_args()}}, \code{\link[=helper_metadata_exif_write]{helper_metadata_exif_write()}}
}
