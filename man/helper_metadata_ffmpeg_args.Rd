% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/metadata_helpers.R
\name{helper_metadata_ffmpeg_args}
\alias{helper_metadata_ffmpeg_args}
\title{Build ffmpeg metadata arguments for cut files}
\usage{
helper_metadata_ffmpeg_args(sourcePath, startsec, clipID, fps = NULL)
}
\arguments{
\item{sourcePath}{Character string. Full path to the source media file.}

\item{startsec}{Numeric. Start time in original source media, in seconds.}

\item{clipID}{Character string. Unique cut identifier (e.g. transcript
name for cuts from a corpus, output filename stem otherwise).}

\item{fps}{Numeric or \code{NULL}. Frame rate of the source video. When
supplied a \verb{-timecode HH:MM:SS:FF} argument is added. \code{NULL} (default)
omits the timecode track (suitable for audio-only outputs).}
}
\value{
Character string with \verb{-metadata comment="..."} and optionally
\verb{-timecode "HH:MM:SS:FF"}. Returns \code{""} if metadata writing is
disabled.
}
\description{
Returns a string that can be appended to ffmpeg command lines to write
\verb{act.*} tags into the comment field of the output file. For video files
(when \code{fps} is supplied) a native timecode track is added via the
\code{-timecode} option.
}
\details{
Returns an empty string when option \code{act.ffmpeg.write_metadata} is
\code{FALSE}, allowing callers to disable tagging globally without changing
call sites.

Tag format: \verb{act.source=<path>;act.startsec=<sec_3nks>;act.clipID=<id>}.
}
\seealso{
\code{\link[=media_metadata_read]{media_metadata_read()}}, \code{\link[=helper_metadata_exif_write]{helper_metadata_exif_write()}}
}
