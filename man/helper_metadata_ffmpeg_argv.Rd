% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/metadata_helpers.R
\name{helper_metadata_ffmpeg_argv}
\alias{helper_metadata_ffmpeg_argv}
\title{Build ffmpeg metadata arguments for cut files as an argument vector}
\usage{
helper_metadata_ffmpeg_argv(sourcePath, startsec, clipID, fps = NULL)
}
\arguments{
\item{sourcePath}{Character string. Full path to the source media file.}

\item{startsec}{Numeric. Start time in original source media, in seconds.}

\item{clipID}{Character string. Unique cut identifier (e.g. transcript
name for cuts from a corpus, output filename stem otherwise).}

\item{fps}{Numeric or \code{NULL}. Frame rate of the source video. When
supplied a \verb{-timecode HH:MM:SS:FF} argument is added. \code{NULL} (default)
omits the timecode track (suitable for audio-only outputs).}
}
\value{
Character vector of ffmpeg arguments, or \code{character(0)} if metadata
writing is disabled.
}
\description{
Same tags as \code{\link[=helper_metadata_ffmpeg_args]{helper_metadata_ffmpeg_args()}}, but returned UNQUOTED as a
character vector, ready for \code{system2()} / an ffmpeg argument list. Callers
that assemble commands as vectors must use this variant: the comment carries
the source path, which may contain spaces, so splitting the quoted string
from \code{\link[=helper_metadata_ffmpeg_args]{helper_metadata_ffmpeg_args()}} again is not safe.
}
\details{
Returns \code{character(0)} when option \code{act.ffmpeg.write_metadata} is \code{FALSE}.
}
\seealso{
\code{\link[=helper_metadata_ffmpeg_args]{helper_metadata_ffmpeg_args()}}, \code{\link[=media_metadata_read]{media_metadata_read()}}
}
